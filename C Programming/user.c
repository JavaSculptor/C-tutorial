//wap to accept no from user and display square
#include<stdio.h>
int main(){
    int x,y;
    printf("Enter a number:");
    scanf("%d",&x);
    y=x*x;
    printf("square of %d is %d",x,y);
    return 0;
}